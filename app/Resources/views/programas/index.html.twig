{% extends '::index.html.twig' %}
{% block contenido %}
    <h1>Programas list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Facultad</th>
                <th>Estado</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for programa in programas %}
            <tr>
                <td><a href="{{ path('programas_show', { 'id': programa.id }) }}">{{ programa.id }}</a></td>
                <td>{{ programa.nombre }}</td>
                <td>{{ programa.facultad }}</td>
                <td>{{ programa.estado }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('programas_show', { 'id': programa.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('programas_edit', { 'id': programa.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('programas_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
